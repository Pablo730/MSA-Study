server:
  port: 0

spring:
  application:
    name: msa-user-service
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: admin
    password: admin
#  config:
#    import: "optional:configserver:http://localhost:8888"
#  h2:
#    console:
#      enabled: true
#      settings:
#        web-allow-others: true
#      path: /h2-console
  datasource:
    url: jdbc:mysql://localhost:3306/msa-user-1
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    database: mysql
    defer-datasource-initialization: true
  security:
    user:
      name: test
      password: $2a$10$MfHrLw/sjpTqrW2Hveg9uu0WyDP3.Z4uuj9lnJdn5UJ1CLs5pnski
#  redis:
#    host: localhost
#    port: 6379
#    redisson:
#      config:
#        singleServerConfig:
#          address: "redis://127.0.0.1:6379"


eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true

greeting:
  message: Welcome to the Simple E-commerce - 2nd.

token:
  secret: my-very-long-secret-key-1234567890123456
  expiration-time: 86400000 # 10 days

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans, info, busrefresh, metrics, prometheus
  tracing:
    sampling:
      probability: 1.0
    propagation:
      consume: B3
      produce: B3
  zipkin:
    tracing:
      endpoint: "http://localhost:9411/api/v2/spans"

#logging:
#  pattern:
#    level: "%5p [%X{traceId:-},%X{spanId:-}]"

#logging:
#  level:
#    org.springframework.security: DEBUG
#    study.msa.msauserservice.client: DEBUG

