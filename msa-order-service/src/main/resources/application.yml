server:
  port: 0

spring:
  application:
    name: msa-order-service
  datasource:
    url: jdbc:mysql://localhost:3306/msa-order-1
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    database: mysql
    defer-datasource-initialization: true
  security:
    user:
      name: test
      password: $2a$10$MfHrLw/sjpTqrW2Hveg9uu0WyDP3.Z4uuj9lnJdn5UJ1CLs5pnski


eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans, info, busrefresh, metrics, prometheus
  tracing:
    sampling:
      probability: 1.0
    propagation:
      consume: B3
      produce: B3
  zipkin:
    tracing:
      endpoint: "http://localhost:9411/api/v2/spans"

#logging:
#  pattern:
#    level: "%5p [%X{traceId:-},%X{spanId:-}]"

#logging:
#  level:
#    study.msa.msaorderservice.client: DEBUG
