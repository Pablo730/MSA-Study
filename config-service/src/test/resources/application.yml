server:
  port: 8888

spring:
  application:
    name: config-service-test
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
  profiles:
    active: git
  cloud:
    config:
      server:
        encrypt:
          enabled: false
        git:
          uri: https://github.com/Pablo730/spring-cloud-config
          default-label: main

spring.cloud.bus.enabled: false
